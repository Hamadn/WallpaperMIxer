<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WallpaperMixer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WallpaperMixer" parent="Base.Theme.WallpaperMixer" />
    
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.WallpaperMixer</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
    </style>
</resources>